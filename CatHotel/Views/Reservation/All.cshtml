@model IEnumerable<ReservationViewModel>

@{
    ViewData["Title"] = "All";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center text-secondary my-sm-3">All Reservations</h2>
<hr />
<div class="row">
    @foreach (var res in Model)
    {
        <div class="col-md-4">
            <div class="card mb-3 text-center">
                <img class="card-img-top mx-auto" style="max-width: 60%; height: auto" src="~/images/reservation.png"  alt="Reservation picture">
                <div class="card-body">
                    <h5 class="card-title">Reservation</h5>
                    <p class="card-text">Arrival: @res.Arrival</p>
                    <p class="card-text">Departure: @res.Departure</p>
                    <p class="card-text font-weight-bold">Cats:</p>
                    @foreach (var cat in res.Cats)
                    {
                        <p class="card-text">@cat.Name - @cat.BreedName</p>
                    }
                    <a asp-controller="Reservation" asp-action="Details" asp-route-resId="@res.Id" class="btn primary-color">Details</a>
                </div>
            </div>
        </div>
    }
</div>

<p class="text-center">
    <a asp-action="Create" class="btn btn-secondary my-sm-3">Create New</a>
</p>